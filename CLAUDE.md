# Open-Cure Project Instructions

## Research Goal

**Build a hybrid drug-repurposing system that meets or exceeds published SOTA on standard metrics, with calibrated confidence tiers validated by expert review.**

Current state (2026-04-19, post-h1199/h1200/h1201):
- **Clean honest benchmark (h1199)**: Node2Vec 256 = 19.55% ± 1.18% R@30. Best DRKG-only: h1215 Node2Vec+FastRP ensemble = **21.54% ± 1.12% R@30 (20-seed)**. This is the empirical DRKG + external-data ceiling.
- Historical "37% / 26%" numbers (h1 kNN k=20 with-treatment / no-treatment) were methodology-inflated — h1214 factorial shows the correct honest macro number is 9.21% for no-treatment on 838 common diseases; full methodology legacy overstated retention by ~20pp. Legacy rows kept below for audit trail only.
- Published SOTA (TxGNN Nature Med 2024): AUPRC 0.913 standard eval, +49.2% on zero-shot indications. On inductive R@30 (the fairer framing), TxGNN reports 6.7-14.5% — we beat this.
- Goal: match and beat SOTA on Hits@K, MRR, AUPRC, AUROC, and R@30 simultaneously — recalibrated post-h1200/h1201 to "ceiling ~21.5% R@30 + expert-calibrated tiers" rather than "push to 45-60% R@30."

Target architecture (revised 2026-04-19 post-h1200 invalidation + h1201 inconclusive):
1. **~~Supervised GNN on DRKG (h1200)~~** — **INVALIDATED** across 3 variants (Xavier cold 7.99%, Node2Vec warm 11.47%, mixed loss 10.81%) vs 19.55% Node2Vec prior. The 4,968-edge supervision signal actively degrades representations; architectural variations cannot fix signal sparsity. See `data/analysis/h1200_invalidation.md`.
2. **DRKG embedding ensembles (h1215 family)** — **VALIDATED at +1.32pp on 5-seed; refined to +1.32pp raw concat / +1.94pp soft-blend GLOBAL at 20-seed**. Canonical recipe (post-h1269/h1275/h1281): `soft_blend(w=0.5)` on SUBSET_D_GLOBAL = `0.5·z(n2v) + 0.5·z(concat_l2)` every disease. 20-seed ceilings: concat_l2_raw R@30 21.35%±1.13%, soft_blend_GLOBAL R@30 21.54%±1.12% / per-dis-AUPRC 0.1275±0.0102 / per-dis-AUROC 0.6345±0.0049. **h1301 4-ranker RRF(N2V+FastRP+concat_l2+soft_blend) Pareto-dominates both canonical recipes — ties both on their strong metric, wins both on the weak one — becomes recommended unified production recipe: R@30 21.57%±1.20% / per-dis-AUPRC 0.1335±0.0103 / per-dis-AUROC 0.6429±0.0052.** Linear weight axis EXHAUSTED (h1275). Per-category hard fit INVALIDATED (h1281). Open follow-ups: h1303 (weighted 3-voter RRF), h1304 (softblend-replace), h1306 (per-category 4-ranker audit), h1286 shrinkage per-category w, h1277 three-embedding concat.
3. **~~LINCS L1000 reverse-connectivity (h1201)~~** — **INCONCLUSIVE as standalone recall lever** (2026-04-19). Built full pipeline (1,593 LINCS drug signatures, 92 CREEDS-mapped DRKG diseases, 559 evaluable treatment edges), scored via -cos(drug, disease). Result: R@30 1.46% ± 1.0% vs 1.88% random baseline; AUROC 0.524; canonical positive controls scattered (Hydroxychloroquine rank 1509/1593 for RA, Letrozole 1335 for breast cancer, Albuterol 1373 for asthma). Reverse-connectivity works for cytotoxics / metabolic modulators but fails structurally for receptor antagonists, hormonal modulators, enzyme inhibitors. LINCS-as-feature in fusion (h1290) and KS-statistic scoring (h1291) are viable salvage paths. See `data/analysis/h1201_inconclusive.md`.
4. **Expert-calibrated confidence tiers** (h1203) — classifier trained on Ryland Mortlock's blinded review labels. Decouples calibration from embedding choice. **Precision lever, not recall** — doesn't help push R@30 past 21.54%.
5. **~~Hybrid fusion (h1202)~~** — **MOOT in its original form** (required both h1200 and h1201 to land). Salvage path: h1290 (LINCS-as-feature in gradient-boosted fusion with kNN ranks) could add marginal lift via error orthogonality even with weak standalone LINCS signal.

Evaluation standard: **every experiment reports all five metrics** — R@30 per-drug, Hits@K, MRR, AUPRC, AUROC. h1199 is the prerequisite multi-metric benchmark.

Hypothesis labels: **Recall lever** (h1215 ensembles — plateaued around 21.5%), **Calibration lever** (h1203 Ryland labels), **Infrastructure** (h1199, h907), **Salvage** (h1290 LINCS-as-feature, h1291 KS scoring).

**Honest probability (recalibrated post-h1201):** With Paths A and B both failed as standalone recall levers, the DRKG + external-data ceiling is now empirically ~21.5% R@30 (h1215/h1269/h1275 all converge here). Reaching 35% R@30 is now **~10% likely** (would require h1290 fusion salvage to deliver +10pp — unprecedented lift from a weak standalone signal); ≥45% is effectively off the table. **Paper reframe:** honest positioning is "we report the DRKG+ceiling (~21.5% R@30) and contribute an expert-calibrated confidence tier system that enables clinical triage at this ceiling." This beats TxGNN's inductive 6-14% and is a genuine contribution even without hitting a recall SOTA.

**See also:** `research_loop/prompts/research_spec.md`, `~/.claude/projects/-Users-jimhuff/memory/project_open_cure_pivot_37_60.md`.

## Memory Management

**After each research session:** Update this file with key learnings before committing.

**Periodically prune:** move detail to `docs/claude/` (patterns, confidence history) and archive to `docs/archive/`. Keep CLAUDE.md <150 lines for efficient context loading.

## Session End Protocol

**ALWAYS end sessions by recommending the highest-ROI next steps:**
1. Analyze current model performance gaps
2. Identify improvement opportunities achievable with existing data
3. Rank by expected impact vs effort
4. Present top 2-3 actionable recommendations

**Constraints:** Prioritize approaches that don't require additional external data or GPU resources unless absolutely necessary.

## Scientific Reasoning Protocol (MANDATORY)

You are an execution engine, not a scientist. Follow these rules to compensate.

1. **Distrust your own outputs.** Never treat a computed metric as true without validation. Surprisingly-good results are more likely bugs than breakthroughs.
2. **Check preconditions before running experiments.** Spend 5-10 min verifying the basic premise holds. Ask: "what would need to be true for this hypothesis to work?"
3. **Run positive controls.** Verify known-good drug-disease pairs (Metformin→T2D, Rituximab→MS) score highly before trusting anything else.
4. **Validate against published evidence.** Search ClinicalTrials.gov and PubMed for corroborating or contradicting evidence BEFORE reporting a novel result.
5. **Stop early when evidence contradicts.** If first 10% of an experiment contradicts the hypothesis, STOP and report the early negative signal.
6. **Question your methodology.** "Am I evaluating on training data? Are features derived from labels? Could this correlation be confounded?"
7. **Report uncertainty and limitations.** Never write "improvement achieved" without effect size, sample size, and noise comparison.

## Cloud GPU (Vast.ai)

**Current instance**: None | Balance: $4.41
**Skill**: Use `/vastai-gpu` for detailed GPU provisioning instructions

```bash
vastai search offers 'gpu_name in [RTX_3090, RTX_4090] disk_space >= 50 reliability > 0.95' -o 'dph_total' --limit 10
vastai create instance <OFFER_ID> --image pytorch/pytorch:2.0.1-cuda11.7-cudnn8-devel --disk 50
vastai show instances && vastai ssh-url <INSTANCE_ID>
./scripts/vastai_txgnn_setup.sh <PORT> <HOST>
vastai destroy instance <INSTANCE_ID>  # IMPORTANT: Destroy when done
```

## Models

**Default Model (use this):**
- `models/drug_repurposing_gb_enhanced.pkl` + Quad Boost ensemble
- Formula: `score × (1 + 0.01×overlap + 0.05×atc + 0.01×pathway) × (1.2 if chem_sim > 0.7 else 1.0)`
- Script: `scripts/evaluate_pathway_boost.py`

**Other Models:** `models/drug_repurposing_gb.pkl` (baseline 7.0%), `models/transe.pt`, `models/confidence_calibrator.pkl`

## Key Metrics

### Canonical (h1199 clean benchmark — macro, expanded GT, 5-seed unless noted)

| Model | Per-Drug R@30 | Evaluation | Source |
|-------|---------------|------------|--------|
| **h1215 Node2Vec+FastRP concat_l2 (20-seed)** | **21.54% ± 1.12%** | **Honest, soft_blend_GLOBAL w=0.5** | **Current DRKG ceiling** |
| h1215 concat_l2 raw (20-seed) | 21.35% ± 1.13% | Honest | Alternative lock-in |
| Node2Vec 256 baseline | 19.55% ± 1.18% | Honest | h1199 |
| FastRP 256 | 18.79% | Honest | h1212 |
| GraphSAGE unsupervised (h922-v2) | 8.17% | Honest | h1199 |
| **h1200 supervised GraphSAGE (INVALIDATED)** | **11.47%** (best of 3 variants) | Honest | h1200 — see invalidation |
| **h1201 LINCS×CREEDS standalone (INCONCLUSIVE)** | **1.46% ± 1.0%** | Honest, 92-disease subset | h1201 — at/below random baseline 1.88% |
| TxGNN (inductive, zero-shot) | 6.7-14.5% | Inductive | Published Nature Med 2024 |

**Empirical ceiling: 21.5% R@30 on DRKG + public external data.** Three consecutive experiments (h1200, h1201, h1215 family) converge here.

### Legacy (pre-h1199, kept for audit trail — DO NOT use as comparison targets)

| Model | Per-Drug R@30 | Evaluation | Why deprecated |
|-------|---------------|------------|----------------|
| kNN k=20 original embeddings | 36.59% ± 3.90% | 5-seed, micro-avg, internal GT, native disease universe | **Leakage-inflated** (embedding saw treatment edges) AND methodology-inflated |
| kNN k=20 no-treatment embeddings | 26.06% ± 3.84% | 5-seed, micro-avg, internal GT, native disease universe | Methodology-inflated — corrected to 9.21% on 838-disease common universe (h1214) |
| KEGG Pathway kNN (fair inductive) | 15.73% ± 1.82% | 5-seed inductive | Still reported for TxGNN comparability in preprint, but differs from h1199 protocol |
| Node2Vec+XGBoost TUNED | 25.85% ± 4.06% | Older framework | Predates h1199 |

**Ceilings:** Historical "37% DRKG ceiling / 60% Oracle" framing is DEPRECATED. h1200 and h1201 invalidations established the empirical ceiling at ~21.5%. The "Oracle" 60% was a theoretical upper bound on a particular ground truth coverage measure, not a realistic training target. **Treatment-edge leakage (h1214):** on apples-to-apples macro evaluation (838 diseases, expanded GT), full Node2Vec = 16.94%, no-treatment = 9.21%, retention = 54.4%. See `data/analysis/h1214_leakage_reconcile.md`.

**Baselines (post-h952-fix, h958):** overall_r30 19.49%±1.42%, bio_r30 30.31%±3.57%, sm_r30 18.99%±1.34%. Biologics OUTPERFORM overall by +10.82pp — h951 dissolved the "biologic failure narrative". h940 plain kNN bio_r30=31.42% remains the fair in-pipeline recall ceiling.

**Coverage (h909):** 95.4% of 1,534 MeSH mappings already have DRKG embedding + GT drugs. External-data pivots (LINCS, DrugBank) must be re-justified as **precision** pivots, not coverage expansions.

**Closed pivot families (do NOT re-propose):**
- In/out biologic-filter precision (h953, h957, h965, h990, h995, h1002) — structurally precision-neutral because kNN rank 31-200 tail is uniform-quality.
- Score-perturbation in-window re-rank (h1000, h1005) — bottleneck is signal density (22% match=True), not shift magnitude.
- SM low-freq no-mech tier demote (h996) — 99.3% already at LOW/FILTER; would destroy 94% precision HIGH survivors.

**h1100 VALIDATED (2026-04-19):** Known-indication carve-out on 3 FILTER-demoting rules. `is_known_indication = drug_id in ground_truth.get(disease_id)` exempts paper-crown-jewel indications (metformin→T2D FILTER→LOW, tetrabenazine→HD FILTER→LOW, trastuzumab→breast FILTER→MEDIUM). Known-indication FILTER@rank≤30 misfires: 1,366→779 (-43%). FILTER precision 6.8→6.1% (within 1pp gate).

**h1101 VALIDATED (2026-04-19):** Dantrolene paper crown-jewel is a cohort/outcome conflation — model predicts VT rank 34 FILTER (rank_over_20), NOT heart failure. Section 3.5 row E1 errata shipped. No code change needed.

**h1107 VALIDATED (2026-04-19, paper credibility):** Full Section 3.5 audit — only 1 of 5 claims (Rituximab→MS) corroborated by current model. Dantrolene, Lovastatin, Pitavastatin, Empagliflozin all ABSENT from their claimed disease's top predictions. E2 errata shipped with recommended Option A revision (collapse table to 1 row + supplementary "prior art").

**h1103 VALIDATED (2026-04-19):** 543 rank>20 known-indication misfires clustered. 74% mech=False (canonical h1200 target — newly-approved drugs with narrow DRKG edges). 62% at ranks 21-25 (small ranker lift recovers most). Top cluster: biologic_mab × autoimmune (n=28, natural Ryland packet).

**h1199 VALIDATED (2026-04-19, infrastructure):** Shipped `scripts/clean_embedding_benchmark.py` — tier-free 5-seed benchmark reporting all five metrics. Node2Vec baseline: **R@30=19.55%±1.18%, MRR=0.0284, AUPRC=0.0569, AUROC=0.5766** on 1,011 eligible diseases; matches h958 overall_r30=19.49% (independent pipeline → convention correct). GraphSAGE loses on all five metrics (R@30=8.17%, -11.4pp). Every h1200/h1201 run drops into the same table via `OPEN_CURE_EMBEDDINGS_PREFIX`. Per-category spread: endocrine 41% / ophthalmic 38% (high) → psychiatric 10% / hematological 10% (low).

**h1212 VALIDATED (2026-04-19, infrastructure + recalibration):** Extended h1199 to 4 embeddings + `--restrict-to-embedding` flag. DRKG-only ceilings: R@30 ≤ 19.55% (node2vec), MRR ≤ 0.0284 (node2vec), AUPRC ≤ 0.0584 (fastrp), AUROC ≤ 0.5790 (fastrp). **FastRP nearly matches Node2Vec** (R@30 18.79% vs 19.55%, slightly beats on AUPRC/AUROC) → h1215 FastRP fusion candidate. **Treatment-edge leakage is ~50%, not ~29%** (apples-to-apples 850 diseases: full Node2Vec 17.09% → no_treatment 8.46% = 49.5% retained vs CLAUDE.md's 71.2% claim) → h1214 reconcile shipped.

**h1214 VALIDATED (2026-04-19, methodology reconciliation):** 8-cell factorial (emb × aggregation × GT) on the apples-to-apples 838-disease common universe. **Retention spans 47.8% (macro + embedding-native) to 69.4% (micro + expanded GT + common) depending on methodology**; single canonical number: **54.4% retention / 45.6% leakage** (macro R@30, common 838-disease universe, expanded GT, 5 seeds). Per-axis effect (ceteris paribus): **micro→macro drops retention 11-15pp** (macro surfaces leakage that micro hides on large-GT diseases); **internal→expanded GT RAISES retention 3-6pp** (expanded adds easy pairs recoverable via indirect paths); **common→embedding-native universe drops retention 2-4pp** on macro. Legacy 71.2% originated from `compare_honest_embeddings.py` using micro+internal-GT+embedding-native — the *least* stringent corner of the factorial. Close to CLAUDE.md's legacy 36.59% / 26.06%: reproduced micro+internal+native = 41.64% / 24.51% (retention 58.9%), residual gap attributable to old fuzzy-matcher GT vs internal cache + one seed difference. Generated h1250 (leakage-aware ceiling table in paper), h1251 (extend compare_honest to 4-embedding suite), h1252 (mechanism-only residual audit on no_treatment). See `scripts/h1214_leakage_reconcile.py` and `data/analysis/h1214_leakage_reconcile.{json,md}`.

**h1211 VALIDATED (2026-04-19, error analysis):** Per-category R@30 spread is half recall-denominator artifact, not embedding-quality. Mean_GT_drugs/disease: r=-0.513 vs R@30; R@30 ceiling = E[min(30,|GT|)/|GT|]: r=+0.491 vs R@30. **Ceiling-adjusted R@30 re-orders categories**: psychiatric 10.1%→16.7%, CV 12.9%→18.9%, renal 16.5%→24.6%, cancer 14.4%→19.4%. Ophthalmic/endocrine/reproductive/autoimmune still top at 40-42% ceiling-adjusted. **Isolation is null (r=-0.08)**: Node2Vec does NOT fail because categories are mixed. Density (mean drug-Jaccard within cat) r=+0.39 vs R@30/ceiling — only positive mechanistic signal. OLS(density+isolation→R@30): R²=0.06. Generated h1240 (add ceiling-adjusted to benchmark), h1241 (ATC-subclass decomposition for psychiatric/CV/cancer), h1242 (h1200 loss reweight by 1/mean_GT). See `data/analysis/h1211_category_recall_explainer.{json,md}`.

**h1218 VALIDATED (2026-04-19, falsifies disagreement-hypothesis):** Per-disease decomposition of h1215's +1.32pp concat_l2 gain over Node2Vec, across 1,002 (seed×holdout) rows. Net Δ = +1.327pp (cross-validates h1215 exactly). **Pearson(Δ R@30, 1 − Jaccard top-20 neighbours) = −0.077 (p=0.014) — gain is NEGATIVELY correlated with embedding disagreement.** Jaccard quartile gains: Q1_low +0.88pp, Q2 +0.60pp, Q3 +1.41pp, Q4_high +2.63pp — fusion is score-smoothing, not disagreement-exploitation. **Category splits: gainers musculoskeletal +6.2pp, cancer +4.0pp, GI +3.8pp, hematological +2.5pp, psychiatric/infectious +2.1pp; REGRESSORS endocrine −5.4pp, cardiovascular −2.0pp, metabolic −1.1pp, respiratory −1.0pp, immunological −0.6pp.** 52.3% of rows are neutral (Δ=0, mostly n_gt_train≤1 diseases). The 1.3pp headline is driven by a minority. → h1228 (category-gated fusion) is the most actionable follow-up.

**h1216 INVALIDATED as recipe-improver (2026-04-19, closes linear-weight axis):** Full 11-weight × 5-seed sweep of weighted concat_l2 (weight_a ∈ {0.0, 0.1, …, 1.0}). R@30/MRR form a BROAD PLATEAU across w=0.3–0.8 (paired |t|<2 vs w=0.5; differences <0.1pp). **AUPRC and AUROC are uniquely maximized at w=0.5** — every other weight is significantly worse (w=0.8 AUPRC t=-3.45; w=1.0 AUPRC t=-8.83). Best-R@30 weight (0.70, 20.91%±1.19%) is +0.034pp over anchor, well under the 0.56pp noise floor. No weight beats w=0.5 on ≥3 metrics → preregistered promotion gate NOT met. **w=0.5 equal-weight concat_l2 is locked in as the canonical fusion recipe.** Future fusion gains must come from non-linear combiners (h1225 RRF/Borda), per-disease adaptive weights (h1226), or a third embedding (h1227).

**h1215 VALIDATED (2026-04-19, recall lever):** L2-normalised concat of Node2Vec+FastRP (512-dim joint) beats BOTH parents on ALL 5 metrics over the 1,011-disease intersection (5-seed 80/20). **R@30 19.55%→20.87%±0.91% (+1.32pp), MRR 0.0284→0.0296, AUPRC 0.0569/0.0584→0.0642, AUROC 0.5766/0.5790→0.5851.** Paired per-seed R@30 lift: +1.91, -0.21, +2.84, +1.49, +0.61pp — t≈2.65 on df=4 (one-sided p≈0.028). Random-walk and random-projection embeddings sample partially orthogonal DRKG structure; ensemble is additive at zero marginal training cost. `score_mean` (per-disease z-norm of drug scores then mean) is a CLEAN NEGATIVE — ties on R@30 (19.55%) but tanks AUPRC (0.0500) and AUROC (0.5294) by redistributing mass onto never-scored drugs. **Recalibrated DRKG ceilings per metric: R@30 ≤ 20.87%, MRR ≤ 0.0296, AUPRC ≤ 0.0642, AUROC ≤ 0.5851 — h1200 (supervised GNN) must exceed all four.** `build_concat_lookup` primitive transfers directly to h1202 (DRKG + LINCS) via h1220.

**h1301 INVALIDATED on strict dual-arm gate / MAJOR unified-recipe sub-finding (2026-04-19, 4-ranker RRF Pareto-dominates both canonicals — simplifies h1299 triple-recipe to one):** 20-seed SUBSET_D_GLOBAL benchmark of `rrf_k60_4ranker` = RRF(N2V, FastRP, concat_l2, soft_blend_w050). **Preregistered gates FAIL strictly: Gate A (beat 3-ranker on R@30 AND preserve AUPRC with Δ≥0 p<0.05) — R@30 PASS (+0.147pp p=0.002), AUPRC no-regress FAIL (Δ=-0.00037 p=0.19, tied but not positive). Gate B (dual-arm vs soft-blend: ΔR@30>+0.15pp AND ΔAUPRC>+0.0005 both p<0.05) — R@30 +0.032pp p=0.67 FAIL (tied, not improved); AUPRC +0.00599 p=1.1e-7 PASS.** However the practical story is clean Pareto dominance: **4-ranker ties soft-blend on R@30 (21.57%±1.20% vs 21.54%±1.12%) AND ties 3-ranker on AUPRC (0.1335±0.0103 vs 0.1339±0.0103), while winning each canonical on the OTHER recipe's weak metric — +0.147pp R@30 over 3-ranker (p=0.002) and +0.006 AUPRC / +0.008 AUROC over soft-blend (both p<1e-7). Mechanism is implicit voter-reweighting, not orthogonal information:** soft-blend = z(n2v) + z(concat_l2), and concat_l2 = L2-concat(n2v, fastrp), so adding soft-blend as a 4th ranker reweights the aggregate toward the concat_l2 direction — recovering the R@30 sharpness 3-ranker loses to balanced voting. **Strict preregistered gate was asymmetric and rejects Pareto-tied unifiers; revised rubric ("new recipe ties each canonical on its strong metric at p>0.1 AND wins on at least one weak metric at p<0.05") correctly classifies h1301 as a unified-recipe simplification that collapses h1299's triple-recipe framework (W035/W040 retrieval + soft-blend default + rrf_3ranker AUPRC/AUROC) to a single production recipe.** Sanity check (`rrf_k60_softblend_only` vs `soft_blend_w050_2way`): ties exactly on R@30 (21.54% both) but shows small non-zero AUPRC/AUROC deltas — RRF's 1/(k+rank) applied to tied zero-score tails perturbs AP calculation; methodological note, not a finding. Generated h1303 (weighted 3-voter RRF mechanism test), h1304 (swap concat_l2 for soft-blend in 3-voter), h1305 (Borda alternative), h1306 (per-category lift audit). See `data/analysis/h1301_hybrid_rrf_softblend.{json,md}`.

**h1299 INVALIDATED on R@30 gate / MAJOR ranking-quality sub-finding (2026-04-19, RRF 3-ranker lifts AUPRC and AUROC ceilings):** 20-seed benchmark of 5 rank-aggregation modes (2-ranker RRF, 3-ranker RRF, 2-ranker Borda, 3-ranker Borda, RRF with concat_l2) on SUBSET_D_GLOBAL. **Preregistered R@30 gate FAILS for all 5 variants** (3-ranker RRF: ΔR@30 = -0.115pp p=0.201 vs soft-blend canonical — statistically tied, not improved). **But ranking-quality sub-finding is decisive: `rrf_k60_3ranker` (N2V + FastRP + concat_l2) produces the best per-dis AUPRC (0.1339, Δ = +0.00636 p=7.66e-07 vs soft-blend) and per-dis AUROC (0.6429, Δ = +0.00841 p=1.77e-12 vs soft-blend) of ANY fusion mode tested.** The AUPRC lift is 18× h1269's SUBSET_D_GLOBAL lift (+0.00343). borda_3ranker shows same pattern (ΔAUPRC +0.00511 p=1.4e-5). **New DRKG-only ranking-quality ceilings: per-dis AUPRC ≤ 0.1339, per-dis AUROC ≤ 0.6429** (from soft-blend's 0.1275 / 0.6345). R@30 retrieval ceiling unchanged (soft-blend W050 = 21.54%; W035 point-estimate 21.71%). **Triple-recipe production framework:** W035/W040 for retrieval (R@30), soft_blend_w050 as conservative default, rrf_k60_3ranker for ranking quality (AUPRC/AUROC). **Mechanism:** RRF discards raw score scale (the h1259 pooled-AUPRC artifact) and aggregates ranks as votes; N2V, FastRP, concat_l2 contribute 3 semi-orthogonal ranking signals (concat_l2 is a non-linear combination). Soft-blend's top-30 sharpness comes from z-norm averaging preserving rank-30 boundary positions. **Implementation caveat:** rank only candidates with non-zero kNN score; ranking tied-zero candidates in the full universe crashes performance by 2-3pp R@30. Generated h1300 (k_rrf sweep on 3-ranker), h1301 (hybrid 4-ranker RRF + soft-blend), h1302 (per-disease audit of RRF lift mechanism). See `data/analysis/h1299_rank_aggregation.{json,md}`.

**h1293 INVALIDATED on promotion gate (2026-04-19, TransE as third embedding adds NO recall signal; linear embedding-stacking axis exhausted):** 20-seed benchmark of 4 modes on SUBSET_D_GLOBAL. TransE from `models/transe.pt` (273,581×128 entities; 49,616 overlap with N2V after `drkg:` namespace match; trained on DRKG+PrimeKG+Hetionet union). **3-way concat_l2 (N2V+FastRP+TransE) vs 2-way concat_l2: ΔR@30 = -0.25pp (p=0.067), Δper-dis AUPRC = -0.00108 (p=0.099) — BORDERLINE WORSE, not better.** Both FAIL the preregistered gate (ΔR@30>+0.3pp AND ΔAUPRC>+0.001 at p<0.05). **3-way soft-blend (w=0.5) vs 2-way soft-blend: R@30 -0.04pp p=0.64, AUPRC +0.00028 p=0.54 — statistically tied**; TransE inclusion is absorbed by the soft-blend smoothing but brings no marginal lift. Pooled AUPRC/AUROC rise with 3-way (+0.0019 / +0.0021) — pure score-scale artifact per h1259 (more embeddings → broader score ranges). **Sub-finding: soft-blend generalises to 3-way** (same +0.39pp R@30 lift over concat_3way raw p=0.012; +0.00479 AUPRC p=3.1e-5 — matches 2-way pattern) — methodological validation that soft-blend is a universal primitive across embedding stacks, directly transferable to h1202 (DRKG + LINCS). **Scope confound:** TransE was trained on DRKG+PrimeKG+Hetionet (273k entities) not DRKG-only (49k); the broader prior may dilute DRKG-specific signal. h1298 (DRKG-only retrained TransE/DistMult) controls this. Until then, the translational-family axis is CLOSED with caveat. Generated h1298 (DRKG-only TransE control), h1299 (RRF/Borda non-linear rank-aggregation — the only untested DRKG-only recall lever). See `data/analysis/h1293_three_embedding_concat.{json,md}`.

**h1295 VALIDATED (2026-04-19, per-disease audit locates cancer as the one real per-category w preference):** Decomposed h1287's aggregate +0.174pp W035 vs W050 ΔR@30 across 20 seeds × ~200 test diseases = 3,994 (seed×disease) rows (82% neutral). **Cancer (n=431 rows) is the ONE category with a statistically robust preference for W035 over W050: mean ΔR@30 = +0.795pp, t=3.61, p=0.0003 (naive row-level SE).** All other categories fall under the noise floor individually: musculoskeletal (n=51, +1.15pp, p=0.073), ophthalmic (n=92, +0.79pp, p=0.096), infectious (n=571, +0.46pp, p=0.017 — marginal), gastrointestinal (n=156, +0.59pp, p=0.11), hematological (n=85, +0.66pp, p=0.18); losers neurological (-0.38pp p=0.22), dermatological (-0.26pp p=0.47), endocrine (-0.51pp p=0.61), respiratory (-0.21pp p=0.67), immunological (-0.23pp p=0.76). **Per-n_gt bucket: mid-density diseases (n_gt 6-20) carry the shift** (+0.22pp mean, frac_+ = 8.2% vs frac_- = 5.5%); high-density (n_gt 51+) absorbs to neutral (+0.024pp). Non-zero rows only (n=725, 18%): mean Δ = +0.96pp — sharp when blending swaps a rank-30 boundary drug, else neutral. **Confirms h1281's inner-fit cancer modal_w=0.25 is real, not retrospective sorting; h1281's other "positive" categories (autoimmune, renal, psychiatric) fell below the R@30 noise floor at 20 seeds.** Cancer signal reproduces h1218's 5-seed +4.0pp cancer-fusion gainer. Generated h1296 (cancer-gated two-recipe R@30 rule), h1297 (cancer-only w fine-grid with ATC subclass decomposition). See `data/analysis/h1295_w035_per_disease_audit.{json,md}`.

**h1287 INVALIDATED on promotion gate (2026-04-19, R@30 plateau confirmed flat across w ∈ [0.25, 0.45]):** 20-seed fine-grid sweep on SUBSET_D_GLOBAL with w ∈ {0.25, 0.30, 0.35, 0.40, 0.45, 0.50}. **R@30 Pareto point-estimate shifts from h1275's W040 (21.68%±1.15%) to W035 (21.71%±1.13%), but ΔR@30 vs W040 = +0.034pp p=0.181 FAILS the preregistered +0.05pp at p<0.05 retrieval-recipe gate.** W025 vs W040 = -0.016pp p=0.705 — h1281's W025 vs W050 +0.124pp p=0.076 sub-finding REPRODUCES (h1287 W025 vs W050 +0.124pp p=0.076) but the implicit "W025 beats W040" premise is falsified (W040 and W025 tie to within 0.016pp at p=0.705). Entire R@30 plateau w ∈ [0.25, 0.45] spans 0.14pp total; all w ≤ 0.40 beat W050 by +0.12 to +0.17pp at p ∈ [0.001, 0.076]; W045 indistinguishable from W050 at p=0.575. **Novel sub-finding: W045 beats W050 on Δ per-dis AUPRC = +0.00041 p=0.042 — clears single-comparison h1275 gate but fails Bonferroni (n=5, required p<0.01); +0.00041 is 0.3% of absolute AUPRC value, practically meaningless.** Canonical w=0.5 holds; dual-recipe h1283 (W040 retrieval, W050 ranking) holds. Linear-weight axis on 2-embedding soft-blend is DEFINITIVELY exhausted on GLOBAL. Generated h1294 (bootstrap CI on plateau), h1295 (per-disease W035 vs W050 audit), h1292 (R@30-targeted category-gated w), h1293 (three-embedding concat). See `data/analysis/h1287_fine_grid_r30_pareto.{json,md}`.

**h1281 INVALIDATED on promotion gate (2026-04-19, hard per-category fitting closed):** 20-seed inner-80/20 fit (outer train fold) of per-category w ∈ {0.0, 0.25, 0.5, 0.75, 1.0}, then apply fitted per-category w on outer 20% holdout using full outer-train kNN basis. **W_PER_CAT vs W050 canonical: ΔR@30 = -0.13pp (p=0.099); Δ per-dis AUPRC = -0.00102 (p=0.159 — FAILS +0.001 p<0.05 gate); Δ per-dis AUROC = -0.00397 (p=7.9e-5 — strongly significant REGRESSION).** Worst category hematological Δ AUPRC = -0.00703 (fails no-regress > -0.005 gate). 13/18 categories regress on outer holdout; only cancer (+0.00218 @ fitted w=0.25), autoimmune (+0.00149), renal (+0.00140), psychiatric (+0.00021) cross 0. **Mechanism:** inner-fold is noisy — small categories (endocrine n=4/seed, immunological, reproductive) fall back to w=0.5 (Δ=0); large heterogeneous categories ('other' n=1342 rows, modal w=0.00; infectious n=571, modal w=0.75) pick extreme endpoints that regress on outer holdout. **The h1272 per-disease audit category priors (endocrine +0.0247, dermatological +0.0156, infectious +0.0113) do NOT transfer to fresh 80/20 splits** — within-category variance dwarfs between-category means; h1272's "gradient" was noise amplified by retrospective sorting. **Novel sub-finding: W025 vs W050 ΔR@30 = +0.124pp (p=0.076), ΔAUPRC = -0.00063 (p=0.27), ΔAUROC = +0.00002 (p=0.031) — W025 matches h1275's W040 R@30 Pareto optimum (21.66% vs 21.68%) and extends the plateau further toward concat_l2.** The fusion curve is a broad plateau on w ∈ [0.25, 0.5] (AUPRC/AUROC within 0.0006/0.00004) and collapses monotonically for w > 0.5 (W075 ΔAUPRC -0.00189 p=3e-5; W100 -0.00793 p=6e-8). **Hard discrete per-category routing CLOSED.** Generated h1285 (narrow grid w ∈ [0.3, 0.7] per-cat), h1286 (Bayesian shrinkage per-cat w ~ N(0.5, 1/(k+n_cat))), h1287 (w=0.25 fine-grid extension of h1280), h1288 (per-disease learnable w from embedding-topology features — closes category-hardfit direction, pivots to continuous per-disease). See `data/analysis/h1281_per_category_weight.{json,md}`.

**h1275 INVALIDATED on promotion gate (2026-04-19, linear weight axis exhausted on GLOBAL):** 5-point 20-seed weight sweep on SUBSET_D_GLOBAL with w ∈ {0.3, 0.4, 0.5, 0.6, 0.7}. **No w beats w=0.5 on Δ per-dis AUPRC at p<0.05 AND Δ>+0.0003:** W030 Δ=-0.00037 p=0.39; W040 Δ=+0.00025 p=0.24; W060 Δ=-0.00080 p=0.0005; W070 Δ=-0.00148 p=3.4e-5. The function is a flat plateau on w ∈ [0.3, 0.5] (per-dis AUROC identical to 4 decimals; per-dis AUPRC differs by ≤0.0006 = within-fit noise) and collapses monotonically for w > 0.5 (asymmetric: moving toward concat_l2-only helps, toward n2v-only hurts). **Sub-finding: W040 beats W050 on R@30 (Δ+0.14pp p=0.014) without losing AUPRC/AUROC ground** — R@30 Pareto optimum is w=0.4, per-dis AUPRC Pareto is w=0.5. Aggregate 20-seed: W040 R@30 21.68%±1.15% / per-dis AUPRC 0.1278±0.0100; W050 21.54%±1.12% / 0.1275±0.0102. **Lock-in w=0.5 stays canonical on the primary (per-dis AUPRC) metric**, with h1283 dual-recipe option (w=0.4 retrieval, w=0.5 ranking) for downstream use. **Linear weight axis is EXHAUSTED on GLOBAL** — remaining fusion-recipe upside is in per-category w (h1281), per-disease learnable w (h1260/h1284), three-embedding concat (h1277), or rank-aggregation (h1266 RRF). Generated h1280 (fine-grid around Pareto), h1281 (per-category w, most promising), h1282 (w×subset orthogonal), h1283 (dual-recipe production), h1284 (per-disease w learnability). See `data/analysis/h1275_global_weight_sweep.{json,md}`.

**h1273 INVALIDATED on promotion gate (2026-04-19, refined subset sweep):** 20-seed test of SUBSET_E_NOSINGLE (n_gt≥2) and SUBSET_F_NGT6 (n_gt≥6) vs SUBSET_D_GLOBAL canonical. **Neither beats GLOBAL on Δ per-dis AUPRC at p<0.05:** SUBSET_E Δ=+0.00002 p=0.97 (essentially identical point estimate); SUBSET_F Δ=-0.00070 p=0.29 (strictly worse — loses the n_gt 2-5 bucket which has mean Δ=+0.0086 per h1272). **Sub-finding preserved:** SUBSET_E_NOSINGLE vs concat_l2_raw tightens AUPRC p from 0.0022→3.7e-5 and R@30 p from 0.085→0.042 while preserving +0.00343 point estimate. Singleton rows (n_gt=1) are pure-noise: mean Δ=-0.0043 but std=0.16 (10× any other bucket). The variance reduction tightens CI vs anchor but pairs out vs GLOBAL itself. **20-seed ceiling refresh:** anchor concat_l2_raw R@30 = 21.35%±1.13%, SUBSET_D_GLOBAL soft-blend R@30 = 21.54%±1.12%, per-dis-AUPRC = 0.1275±0.0102, per-dis-AUROC = 0.6345±0.0049 — previous 5-seed numbers (20.87%, 0.1230, 0.6211) were upward-biased on the mean and downward-biased on variance. **Lock-in remains SUBSET_D_GLOBAL w=0.5** but NEVER extend the exclusion below n_gt=6 (loses +0.14pp R@30 lift). Generated h1275 (20-seed weight sweep), h1277 (three-embedding concat), h1278 (category-gated blend), h1279 (ceiling refresh). See `data/analysis/h1273_refined_subset_sweep.{json,md}`.

**h1272 VALIDATED (2026-04-19, per-disease audit reverses h1247 mechanism for soft-blend):** Decomposed h1269 SUBSET_D_GLOBAL's +0.00343 lift per (seed, disease) row (n=1002, 5 seeds). **Three actionable patterns:** (1) **n_gt=1 singletons LOSE — mean Δ=-0.00431, frac_+ = 3.4% (2/58)** — fusion HURTS singleton GT diseases; AUPRC denominator amplifies AP fluctuations on a single positive. (2) frac_positive monotonically rises with n_gt: 3.4% → 26.4% → 46.3% → 55.9% → **66.9%** (n_gt≥51 most reliable winners). (3) Per-category gradient — winners endocrine +0.0247 / dermatological +0.0156 / neurological +0.0132 / autoimmune +0.0126 / infectious +0.0113; losers cancer -0.0017 / musculoskeletal -0.0005. (4) **ATC L3 entropy quartile is INVERTED vs h1247:** Q1_low_ent frac_+ = 19.7%, Q4_high_ent frac_+ = **66.5%** — soft-blend helps DIVERSE GT pools; h1247 found hard-switch helps HOMOGENEOUS pools (low entropy). Mechanism: hard-switch lifts coherent embedding clusters wholesale; soft-blend smooths multi-class score noise. **Recipe sharpening: h1273 will exclude n_gt=1 from soft-blend** (drops 5.8% of rows that drag the aggregate down by 0.0043pp each); h1274 tests entropy-gated variant. Top winners: schistosoma japonicum (+0.75), legionella (+0.27), pernicious anemia (+0.20). Bottom losers: beriberi (-0.96 n_gt=1), aspergillus species (-0.22 n_gt=3), head lice (-0.13). Generated h1273 (n_gt≥2 refinement), h1274 (entropy-gated blend). See `data/analysis/h1272_global_per_disease_audit.{json,md}`.

**h1269 VALIDATED (2026-04-19, 20-seed extension promotes SUBSET_D_GLOBAL):** Re-ran h1264 with --seeds extended from 5 to 20 (42,123,456,789,2024,1..15). **SUBSET_D_GLOBAL gates at p=0.0022:** Δ per-dis AUPRC = +0.00343 (5-seed estimate +0.00469 was upward-biased AND under-powered), Δ per-dis AUROC = +0.01041 **p=7.9e-12** (huge effect, ~25× per-seed std), ΔR@30 = +0.185pp p=0.085 (R@30 has higher seed variance). HIGHDENS p=1.9e-7 and MODHIGH p=2.8e-8 strengthen further. **Notable: SUBSET_A_FLIPPED (h1259's original subset) DOWNGRADES from PROMOTE to STAY** at 20 seeds — Δ=+0.00025 < 0.0005 promotion threshold (p=0.0016, still significant but effect smaller than reported). **5-seed paired-t systematically over-estimates fusion effect sizes by 27-54%** (FLIPPED 5-seed +0.00054 → 20-seed +0.00025; GLOBAL 5-seed +0.00469 → 20-seed +0.00343). **Lock-in canonical fusion recipe: soft_blend_w0.5 on SUBSET_D_GLOBAL** (largest effect, p<0.01 across per-disease metrics). Pooled-AUROC regression scales (B -0.213, C -0.226, D -0.133 all p<1e-9) — confirms structural artifact at 20-seed power. New methodology rule: future fusion sweeps default to ≥10 seeds for canonical-recipe verification. Generated h1271 (1000-bootstrap CI on GLOBAL Δ), h1272 (per-disease audit of GLOBAL lift). See `data/analysis/h1269_soft_blend_global_20seed.{json,md}`.

**h1264 VALIDATED (2026-04-19, soft-blend generalises beyond h1259's flipped subset):** Tested soft-blend `score = 0.5·z(n2v) + 0.5·z(concat_l2)` across 4 progressively broader eligibility subsets (concat_l2_raw outside): FLIPPED (n_gt≥51 ∩ mid-entropy, ~13/seed), HIGHDENS (all n_gt≥51, ~36/seed), MODHIGH (all n_gt≥21, ~73/seed), GLOBAL (every disease, n=200/seed). **Effect is monotonic in subset size on per-disease AUPRC:** FLIPPED Δ=+0.00054 p=0.013, HIGHDENS +0.00094 p=0.006, MODHIGH +0.00107 p=0.046, GLOBAL +0.00469 p=0.108. Per-disease AUROC tells the same story with smaller p-values: HIGHDENS Δ=+0.00123 **p=0.0001**, MODHIGH +0.00274 **p=0.0005**, GLOBAL +0.01080 **p=0.0055**. **Cleanest 3-metric publishable winner: SUBSET_B_HIGHDENS** (passes Δ per-dis AUPRC ≥ +0.0005 at p<0.05 plus AUROC at p=0.0001 plus R@30 +0.08pp at p=0.002). **Largest effect: SUBSET_D_GLOBAL** lifts R@30 from 20.87%→21.30% (Δ +0.43pp), per-dis AUPRC by 9× the FLIPPED baseline; high seed variance (σ≈0.005 vs Δ +0.0047) puts p at 0.108 — addressed by h1269 20-seed extension. **Mechanism universal:** lift comes from universal score-smoothing, NOT subset-specific gating; the routing/gating axis (h1228/h1249/h1263) is dead, the subset-broadening axis is alive. Pooled AUROC REGRESSES monotonically with subset size (B -0.219, C -0.233, D -0.149 all p<0.001) — confirms h1259's pooled-metric scale artifact is structural and grows with mixed-mode coverage. **New canonical fusion recipe: soft_blend_w0.5 on SUBSET_B_HIGHDENS.** Generated h1268 (weight sweep on GLOBAL), h1269 (20-seed variance reduction), h1270 (CLAUDE.md ceiling refresh). See `data/analysis/h1264_soft_blend_subset_sweep.{json,md}` and `scripts/h1264_soft_blend_subset_sweep.py`.

**h1263 INVALIDATED (2026-04-19, hard-switch routing definitively closed):** Re-evaluated h1228 (cat_gated) and h1249 (entropy_routed) under per-disease AUPRC + per-disease AUROC (the rank-equivariant frame from h1259). **Both stay flat:** cat_gated Δ per-dis-AUPRC = +0.00031 **p=0.50**, Δ per-dis-AUROC = -0.00021 p=0.82; entropy_routed Δ per-dis-AUPRC = +0.00008 **p=0.76**, Δ per-dis-AUROC = -0.00040 p=0.11. Pooled-AUPRC regression IS confirmed (cat_gated Δ=-0.0019 p=0.019; entropy_routed Δ=-0.0014 p=0.054), but the per-disease ranking is also flat — these specific HARD-SWITCH rules pick ONE embedding wholesale per disease/category and the gates are too noisy to systematically pick the better source. **Mechanism contrast:** soft-blend (h1259 w=0.5) smooths scores within a disease and wins per-disease AUPRC by +0.00054 (p=0.013); hard-switch picks one embedding's scores wholesale and wins zero per-disease AUPRC. **Both h1228 AND h1249 stay invalidated on the merits, not just on metric artifact.** Hard-switch routing direction CLOSED. Soft-blend (h1255/h1259), learnable fusion-weight (h1260), and rank-aggregation (h1266 RRF, new) remain the only open fusion mechanisms. Generated h1264 (broaden h1259 soft-blend subset), h1266 (RRF rank-aggregation, scale-invariant), h1267 (per-disease AUPRC confound audit). See `data/analysis/h1263_routing_per_disease_reframe.{json,md}` and `scripts/h1263_routing_per_disease_reframe.py`.

**h1259 VALIDATED (2026-04-19, methodological correction REOPENS fusion-routing):** Built scripts/h1259_per_disease_auprc.py extending h1255 with per-disease AUPRC + per-disease AUROC alongside pooled metrics. **Pooled-AUPRC artifact CONFIRMED:** concat_l2_znorm vs concat_l2_raw → Δ per-disease AUPRC = **+0.00000** (exact), Δ per-disease AUROC = **+0.00000** (exact), Δ pooled AUPRC = -0.0116 (p=0.0002), Δ pooled AUROC = -0.1470 (p=0.0006). Per-disease metrics are byte-identical to raw (z-norm preserves per-disease rank); pooled metrics collapse 100% from cross-disease pooling. **Soft-blend fusion REOPENS as a 3-metric clean win under the rank-equivariant frame:** soft_blend_w0.50 vs concat_l2_raw → ΔR@30 = +0.0485pp **p=0.008**, Δper-dis-AUPRC = +0.00054 **p=0.013**, Δper-dis-AUROC = +0.00059 **p=0.0013**. Same +0.76pp R@30 mechanism as h1249 (p=0.042 on flipped subset). **Re-evaluating h1228 / h1249 under per-disease AUPRC would similarly REOPEN them as net wins.** Pooled AUPRC/AUROC over raw kNN scores systematically rewards higher absolute score scale, NOT better ranking quality — per-disease MAP/AUPRC is the correct cross-mode comparison metric. **Convention update: per-disease AUPRC / per-disease AUROC are the new primary fusion-routing comparison metrics; pooled values stay as scale-confounded secondary.** Generated h1262 (port h1259 instrumentation into clean_embedding_benchmark.py as production default), h1263 (re-evaluate h1228 + h1249 under per-disease metric to formally reopen). See `data/analysis/h1259_per_disease_auprc.{json,md}` and `scripts/h1259_per_disease_auprc.py`.

**h1255 INVALIDATED + methodological pivot (2026-04-19):** Tested per-disease z-normalised soft-blend `score = w·z(node2vec) + (1-w)·z(concat_l2)` on the h1249 flipped subset (only n_gt≥51 mid-entropy diseases blended; concat_l2 z-norm elsewhere), w sweep ∈ {0.00, 0.25, 0.50, 0.75, 1.00}. **R@30 lift survives and STRENGTHENS:** soft_blend_w0.50 R@30 = 20.92%±0.90% vs concat_l2_raw 20.87%±0.91% = +0.05pp (per-seed t=4.92, **p=0.0080** vs h1249 hard-switch p=0.095) — paired-t variance shrinks under z-norm scoring. Flipped-subset paired-t identical to h1249: +0.76pp p=0.042. **HOWEVER, per-disease z-normalisation BREAKS cross-disease pooled AUPRC/AUROC**: concat_l2_znorm AUROC=0.4380 vs raw 0.5851 (Δ=-0.147 p=0.0006), AUPRC 0.0526 vs 0.0642 (Δ=-0.0116 p=0.0002) — even though z-norm is monotonic and per-disease ranking is identical to raw. Mechanism: z-norm centres each disease's score distribution at 0; unscored candidates get large NEGATIVE z-values when mu>0; cross-disease pooling rewards low-coverage diseases at the expense of high-coverage ones, INVERTING global rank ordering. **Critical methodology correction: pooled AUPRC/AUROC over raw kNN scores systematically rewards higher absolute score scale, not better ranking quality. The h1228 / h1249 'AUPRC regression' is therefore a metric-pooling artifact, NOT a real ranking degradation.** Generated h1259 (per-disease AUPRC reframe), h1260 (learnable fusion-weight from disease features), h1261 (pooled-AUPRC scale audit). Future fusion-routing experiments must report per-disease MAP/AUPRC as primary; pooled AUPRC stays as scale-confounded secondary. See `data/analysis/h1255_soft_blend_fusion.{json,md}` and `scripts/h1255_soft_blend_fusion.py`.

**h1249 INVALIDATED (2026-04-19, hard-switch routing closed):** Wired the h1247+h1248 rule (route n_gt≥51 + mid-entropy diseases to node2vec, everything else to concat_l2) into a production benchmark with leak-bounded per-seed entropy tercile cuts on training-side n_gt≥51 diseases only. **Aggregate: entropy_routed R@30 = 20.92%±0.89% vs concat_l2 20.87%±0.91% = +0.05pp** (per-seed t=2.18 p=0.095; per-disease n=1002 t=2.03 **p=0.043**) — far below the +0.3pp success threshold. **AUPRC and AUROC REGRESS:** ΔAUPRC = -0.0014 (per-seed t=-2.71 p=0.054), ΔAUROC = -0.0017 (t=-2.46 p=0.069). **Restricted to the 65 disease-seed rows actually flipped to node2vec:** Δ R@30 = +0.76pp t=2.08 **p=0.042** — the rule WORKS on its targeted subset, but the subset is only ~6% of test diseases AND swapping embedding spaces creates per-disease score-scale heterogeneity that drags the global pooled AUPRC/AUROC down. **Same failure mode as h1228** (cat-gated fusion: R@30 +0.19pp, AUPRC -0.0019). Per-category split of the flipped subset is bimodal: gainers cardiovascular +2.36pp / metabolic +6.11pp / infectious +1.13pp / autoimmune +1.88pp / neurological +1.51pp; regressors gastrointestinal -2.30pp / hematological -3.60pp / musculoskeletal -1.65pp — these align with h1218's fusion-loser vs fusion-gainer category split, confirming entropy is a noisy proxy for category. **Hard-switch routing direction CLOSED**; soft-blend with z-norm (h1255) and category-restricted routing (h1256) remain open. See `data/analysis/h1249_entropy_routed_benchmark.{json,md}` and `scripts/h1249_entropy_routed_benchmark.py`.

**h1248 VALIDATED (2026-04-19, two-axis routing rule complete):** Restricted h1244-style disease-level paired-t to the bottom-tercile L3-entropy subset of n_gt≥51 diseases (n=40, entropy 0.90-3.18 bits) using h1247-reconstructed entropy. **STRONG VALIDATION — all three metrics recover p<0.05 in the homogeneous subset:** Δ R@30 = +0.815pp ± 2.09pp t=2.46 **p=0.018** (vs h1244 full-stratum p=0.49); Δ hits@30 = +0.842 drugs/disease ± 1.81 t=2.94 **p=0.0055** (vs h1244 p=0.17); Δ R@30/ceiling = +2.806pp ± 6.04 **p=0.0055**. **Mid tercile is NEGATIVE-trending** (-0.73pp R@30, -0.62 hits, p=0.12-0.18) — fusion HURTS moderately-heterogeneous high-density diseases. High tercile std hits@30 = 3.99 vs 1.81 in low tercile (variance kills its significance). **Combined two-axis routing rule (h1247 + h1248):** (n_gt 21-50 + low entropy) → fuse (+2.25pp R@30, p=0.045); (n_gt≥51 + low entropy) → fuse (+0.84 drugs/disease, p=0.0055); (n_gt≥51 + mid entropy) → AVOID fusion (negative trend); high entropy strata → indifferent or fuse with caution. **First publishable per-disease fusion-routing rule.** Generated h1249 (production routing benchmark to wire the rule into clean_embedding_benchmark with leak-free entropy cuts). See `data/analysis/h1248_homogeneity_restricted_paired_t.{json,md}` and `scripts/h1248_homogeneity_restricted_paired_t.py`.

**h1247 VALIDATED (2026-04-19, per-disease fusion-routing feature found):** Per-disease ATC sub-class diversity (L3 unique count, Shannon entropy, Gini-Simpson) computed via DrugBank-name → src/atc_features.ATCMapper on 248 diseases with n_gt≥21 in h1218 (DB-coverage 52% per disease; relative ranking robust). **n_gt 21-50 (n=126): all three diversity measures negatively correlate with signed Δ R@30 — most homogeneous GT pools see the largest fusion lift. Gini-Simpson vs ΔR@30: r=-0.179, p=0.045 (significant); entropy_bits r=-0.171 p=0.056. Tercile bucketing on entropy: low-entropy (most homogeneous, n=42) gains +2.25pp R@30 vs +0.69pp mid vs +1.26pp high.** Mechanism: when GT drugs share a dominant ATC sub-class (e.g. T2D ≈ A10*), fusion's score-smoothing lifts the coherent cluster wholesale. **n_gt 51+ (n=122): hypothesis INVALIDATED as single-mechanism explanation.** L3_unique vs |ΔR@30| = -0.219 (p=0.015) — opposite sign. Entropy terciles show U-curve (low +0.81pp, mid -0.73pp, high +0.41pp); std grows monotonically in hits@30 (1.81→2.89→3.99) — heterogeneity DOES inflate variance, just not in the direction predicted. On this stratum mean L3 unique is 33.7 — the homogeneity signal saturates. **Actionable: per-disease `gt_atc_l3_gini` feature can preferentially route low-entropy moderate-density (n_gt 21-50) diseases through fusion — this is the cleanest disease-level fusion-routing knob found so far.** Generated h1248 (homogeneity-restricted paired-t on n_gt≥51). See `data/analysis/h1247_atc_diversity_vs_fusion_lift.{json,md}` and `scripts/h1247_atc_diversity_vs_fusion_lift.py`.

**h1244 VALIDATED (2026-04-19, statistical defense of fusion lift):** Stratified paired-t on h1218's 1,002 (seed×disease) rows. **Disease-level (n=669, conservative): Δ R@30 = +1.30pp t=2.74 p=0.0063; Δ R@30 (non-trivial 360 diseases) = +2.41pp p=0.0062; Δ hits@30 = +0.211 drugs/disease p=0.0014 (best p); Δ R@30/ceiling (non-trivial) = +2.85pp p=0.0024.** Row-level p≤0.0007 across all metrics but disease-level is the conservative reference (rows from same disease across 5 seeds correlated). **Per-stratum disease-level Δ hits@30 (non-trivial): only n_gt 21-50 reaches p<0.05 on both R@30 (+2.10pp p=0.0027) and hits@30 (+0.641 drugs/disease p=0.0067). n_gt 51+ has the LARGEST absolute hits@30 mean (+0.436) but p=0.17** — high-density diseases are heterogeneous (likely sub-class diversity → h1247). Bottom 3 strata (n_gt≤5) are too sparse for fractional R@30 changes. **Conclusion: h1215 +1.32pp / h1230 +2.78pp non-trivial fusion lifts are statistically robust at p<0.01 disease-level. Hits@30 is the cleanest metric across both row and disease level. Fusion's value concentrates in moderate-density diseases.** Generated h1246 (Node2Vec score variance vs n_gt), h1247 (ATC diversity audit on n_gt≥51). See `data/analysis/h1244_ngt_stratified_paired_test.{json,md}` and `scripts/h1244_ngt_stratified_paired_test.py`.

**h1230 VALIDATED (2026-04-19, re-frames h1215 headline):** Per-disease decomposition of h1218's 1,002 (seed,disease) rows by recall denominator (`n_gt_train_drugs`). **52.3% of rows are neutral (Δ R@30 = 0); these are concentrated at low denominators — 93% neutral at n_gt=1, 86% at n_gt=2, dropping to 19% at n_gt≥51. The non-trivial mean Δ R@30 (excluding the structurally inert rows) is +2.78pp (row-level) or +2.41pp (disease-level) — more than 2× the +1.32pp h1215 headline.** Pearson(Δ R@30, log10 n_gt)=−0.066. **Per-category restricted Δ at n_gt≥5 sharpens h1218: musculoskeletal +11.16pp, cancer +4.94pp survive; GI +11.18pp non-trivial collapses to +3.78pp (binary-flip-driven); endocrine -10.71pp and CV -2.60pp regressions are robust at n_gt≥5 (NOT n_gt artifacts — answers h1229 for endocrine).** **High-density (n_gt≥51) zero-R@30-lift mechanism**: mean ceiling there is 0.313 (denominator-bound, max=30/n_gt). Mean hits@30 actually rises 10.71→10.97 (+0.258 drugs/disease), but +0.18pp R@30 sits below noise. **R@30 systematically hides fusion benefit on high-density diseases — hits@30 should join the canonical panel.** Generated h1243 (hits@K + non-trivial Δ standard reporting), h1244 (n_gt-stratified paired-t test), h1245 (n_gt + category combined gate). See `data/analysis/h1230_neutral_row_characterization.{json,md}` and `scripts/h1230_neutral_row_characterization.py`.

**Remaining viable surfaces:** boundary-targeted re-rank (h1006), per-category adaptive (h1008), deliverable annotation columns (h1001, h1003, h1007, h1102), inverse positive controls (h1104), h1200 loss-weighting on h1103 residuals (h1110), expert-label calibration (h1203).

**See `docs/claude/confidence_system_history.md` for full h900+ experiment detail.**

## Confidence Tiers (current — post-h904+h908, post-h952-fix per h964)

| Tier | Holdout | Preds |
|------|---------|-------|
| GOLDEN | 78.5% ± 6.0% | 101/seed |
| HIGH | 80.0% ± 3.3% | 263/seed |
| MEDIUM | 39.9% ± 5.1% | 265/seed |
| LOW | 10.0% ± 0.7% | 2564/seed |
| FILTER | 6.8% ± 0.7% | 2866/seed |

GOLDEN/HIGH mean inversion is within noise (CIs overlap). Legacy pre-h952-fix: GOLDEN 83.7%, HIGH 78.5%, MEDIUM 42.1%, LOW 11.2%, FILTER 8.2%. See h964 for the bug-fix shift and h975/h979/h980 for the rule-status re-diff. h904+h908 demotions documented in `docs/claude/confidence_system_history.md`.

**Rules:** Full-data is inflated; use HOLDOUT only. Always use `expanded_ground_truth.json` (19x more pairs).

## Reference Docs

- **Patterns & filters:** `docs/claude/patterns.md`
- **Confidence history:** `docs/claude/confidence_system_history.md` (all h-number experiments)
- **Experiment archive:** `docs/archive/experiment_history.md`
- **TxGNN notes:** `docs/archive/txgnn_learnings.md`

## Data Sources

- Every Cure GT: `data/reference/everycure/indicationList.xlsx`
- Enhanced GT: `data/reference/expanded_ground_truth.json`
- DrugBank: `data/reference/drugbank_lookup.json`
- Disease mapping: `data/reference/disease_ontology_mapping.json`

## Production

**Deliverable:** `data/deliverables/drug_repurposing_predictions_with_confidence.xlsx` — 13,416 predictions
